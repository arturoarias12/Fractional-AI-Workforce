# Root-level requirements.txt, for hosting platforms that expect one here
# by convention (e.g. Render, Railway). Streamlit Community Cloud does NOT
# use this file when dashboard/requirements.txt also exists - confirmed
# from a real deploy's build log, which picked dashboard/requirements.txt
# instead (the file closest to the app's entry point, dashboard/app.py).
# If deploying to Streamlit Community Cloud, dashboard/requirements.txt is
# the one that actually matters - keep both in sync.
-e .[full-demo,dev]
streamlit>=1.40,<2
requests>=2.31,<3
