[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "fractional-ai-workforce"
version = "0.1.0"
description = "Implementation-ready placeholder scaffold for a multi-agent investment-research system."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
